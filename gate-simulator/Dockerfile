#выбор готового образа для старта, на базе которого мы будем создавать свой;
FROM node:8.16.2-alpine
#установка рабочего каталога внутри образа
WORKDIR /opt/app
# Копирование файлов проекта
COPY ../.. .
# выполние дополнительных действий, например, установку зависимостей;
RUN npm install
# описание команды запуска
CMD ["npm", "start"]
# Уведомление о порте, который будет прослушивать работающее приложение
EXPOSE 9999
